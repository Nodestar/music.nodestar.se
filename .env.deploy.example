# Kopiera till .env.deploy (gitignorad) och fyll i.
FTP_HOST=ftp.example.com
FTP_USER=username
FTP_PASS=password
# Katalog på servern som build/ ska speglas till
FTP_DIR=/public_html
# ftp | ftps (explicit TLS, rekommenderas om servern stödjer det) | sftp
FTP_PROTOCOL=ftps
